# Skill Trust Policy Examples

These examples demonstrate trust controls using `skill_provenance`:

- `allow_trusted.yaml`: allow skill executions from trusted registry publisher.
- `block_untrusted.yaml`: block untrusted publishers.
- `require_approval_untrusted_source.yaml`: require approval for non-registry sources.
