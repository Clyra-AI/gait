default_verdict: block
rules:
  - name: allow-safe-tools
    effect: allow
    match:
      tool_names: [tool.read, tool.list, tool.info]
  - name: block-destructive-tools
    effect: block
    match:
      tool_names: [tool.delete, tool.exec]
