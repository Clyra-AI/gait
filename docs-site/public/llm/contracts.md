# Gait Contracts

Stable OSS contracts include:

- Artifact schemas (`schemas/v1/*`)
- Primitive behavior (`verify`, `diff`, `stub replay` determinism)
- Stable CLI exit codes
- Backward-compatible readers within major version

Reference: `docs/contracts/primitive_contract.md`.
